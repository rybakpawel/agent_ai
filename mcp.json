{
  "name": "bzone_agent",
  "description": "Server for handling B-Zone System tasks",
  "tools": [
    {
      "name": "create_purchase_initiative",
      "description": "Creates a new purchasing initiative with provided name.",
      "input_schema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "The name of the initiative."
          }
        },
        "required": ["name"]
      },
      "endpoint": {
        "url": "/ask-audio",
        "method": "POST",
        "description": "Accepts audio input, sends it to 11.ai, and creates a purchasing initiative if requested."
      }
    }
  ]
}
